export const content = `## ERC20 Part 2

Adding state an initial variables to the contract.

Each token needs certain variables to be set before it can be used.

1. name (string public)
2. symbol (string public)
3. decimals (uint8 public)


Add the those variables to the contract with your own values!

## Once you can compile and deploy the contract, you can move to the next section!
`;
